name: vibe
description: "vibe-mobile app"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.0-290.1.beta

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  get_it: ^9.0.5
  dartssh2: ^2.13.0
  xterm: ^4.0.0
  equatable: ^2.0.7
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  bloc_test: ^10.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13


# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true

